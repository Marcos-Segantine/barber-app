import {View, Text, StyleSheet} from 'react-native';

import {globalStyles} from '../globalStyles';

export const ChoiceLogin = ({navigation}) => {
  return (
    <View style={globalStyles.container}>
        <Text>HELLO</Text>
    </View>
  )
};

const style = StyleSheet.create({});
